# LaVita Yarn Support Bot

Бот поддержки для платформы Max.ru, который автоматически пересылает вопросы клиентов в чат поддержки и позволяет операторам отвечать через Reply механизм.

## 📋 Содержание

- [Обзор](#обзор)
- [Особенности](#особенности)
- [Архитектура](#архитектура)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Использование](#использование)
- [API Reference](#api-reference)
- [Структура проекта](#структура-проекта)

---

## 🎯 Обзор

**LaVita Yarn Support Bot** — это решение для автоматизации обработки обращений клиентов на платформе Max.ru. Бот выступает посредником между клиентами и командой поддержки, обеспечивая:

- ✅ Автоматическую пересылку вопросов клиентов в групповой чат поддержки
- ✅ Простой механизм ответа через Reply (операторы просто нажимают "Ответить")
- ✅ Полную историю переписки в SQLite базе данных
- ✅ Экспорт данных в Excel с одной команды
- ✅ Персонализированные приветствия и подсказки

### Сценарий использования

```
Клиент                    Бот                  Чат поддержки
    |
    |-- "Привет!" -->     |-- Переслает --> |
    |                     |                  | Оператор видит сообщение
    |                     |                  | и нажимает "Ответить"
    |                     |                  |
    |<-- Ответ! ----  |<-- Получает ответ --|
```

---

## ✨ Особенности

### Автоматическая обработка событий
- **Событие `bot_started`** — клиент запустил бота
- **Событие `message_created`** — новое сообщение в чате
- **Маршрутизация по типам** — разные сценарии обработки

### Управление пользователями
- Регистрация новых пользователей
- Отслеживание первого и последнего контакта
- Сохранение истории для каждого пользователя

### Система сообщений
- Сохранение всех сообщений (входящих и исходящих)
- Маппинг между message_id чата и user_id клиента
- Подсчёт статистики ответов оператора

### Экспорт данных
- Генерация Excel файлов с единой команды `/export`
- Отчёты по пользователям и сообщениям
- Форматирование с цветными заголовками и автоширивкой колонок

### Обработка ошибок
- Retry логика при ошибках API
- Таймауты с управляемыми задержками
- Debug режим с трассировкой ошибок

---

## 🏗️ Архитектура

Проект следует принципам **Clean Architecture** с четырёх-слойной моделью:

```
┌─────────────────────────────────────┐
│      Presentation Layer             │
│  (main.py, BotService)              │
├─────────────────────────────────────┤
│      Application Layer              │
│  (UpdateHandler, Services)          │
├─────────────────────────────────────┤
│      Domain Layer                   │
│  (Models: User, Message, Update)    │
├─────────────────────────────────────┤
│      Data Layer                     │
│  (Repositories, DatabaseConnection) │
└─────────────────────────────────────┘
```

### Компоненты

| Компонент | Назначение |
|-----------|-----------|
| **BotService** | Основной цикл работы бота (long polling) |
| **UpdateHandler** | Маршрутизация событий по типам |
| **UserService** | Бизнес-логика управления пользователями |
| **MessageService** | Бизнес-логика управления сообщениями |
| **ExportService** | Генерация отчётов в Excel |
| **MaxApiClient** | Клиент для работы с Max.ru API |
| **SQLiteUserRepository** | Слой доступа к данным пользователей |
| **SQLiteMessageRepository** | Слой доступа к данным сообщений |
| **DatabaseConnection** | Менеджер подключения к БД |

### Поток данных

```
Max.ru API
    ↓
BotService (long polling)
    ↓
UpdateHandler (маршрутизация)
    ↓
UserService / MessageService (бизнес-логика)
    ↓
Repositories (сохранение/получение данных)
    ↓
SQLite Database
```

---

## 🚀 Установка

### Требования
- Python 3.11+
- pip или uv (пакетный менеджер)

### Шаги установки

1. **Клонируйте репозиторий**
```bash
git clone <repository_url>
cd lavita-bot
```

2. **Создайте виртуальное окружение**
```bash
python -m venv venv
source venv/bin/activate  # Linux/macOS
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости**
```bash
pip install requests openpyxl python-dotenv
```

4. **Создайте файл `.env`**
```bash
cp .env.example .env
```

---

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Max.ru API конфигурация
API_TOKEN=your_api_token_here
BASE_URL=https://platform-api.max.ru

# IDs
SUPPORT_CHAT_ID=123456789
BOT_ID=987654321

# Database
DATABASE_PATH=lavita_bot.db

# Long polling
POLLING_TIMEOUT=30
POLLING_REQUEST_TIMEOUT=35

# Поведение
DEBUG=false
MESSAGE_DELAY=0.3
ERROR_RETRY_DELAY=5
```

### Переменные окружения

| Переменная | Описание | Пример |
|-----------|---------|--------|
| `API_TOKEN` | Токен для авторизации в Max.ru API | `Bearer xxxx...` |
| `BASE_URL` | Базовый URL API Max.ru | `https://platform-api.max.ru` |
| `SUPPORT_CHAT_ID` | ID группового чата поддержки | `123456789` |
| `BOT_ID` | ID самого бота | `987654321` |
| `DATABASE_PATH` | Путь к SQLite базе данных | `lavita_bot.db` |
| `POLLING_TIMEOUT` | Таймаут для long polling (сек) | `30` |
| `DEBUG` | Включить debug режим | `true` / `false` |
| `MESSAGE_DELAY` | Задержка между сообщениями (сек) | `0.3` |

---

## 🎮 Использование

### Запуск бота

```bash
python main.py
```

Вывод:
```
✅ База данных инициализирована
🤖 Бот LaVita yarn запущен!
📡 Подключение к https://platform-api.max.ru
💬 Чат поддержки: 123456789
🤖 ID бота: 987654321
──────────────────────────────────────────────────────────
✨ Операторы могут просто нажать 'Ответить' на сообщение!
──────────────────────────────────────────────────────────
```

### Команды для клиентов

| Команда | Описание |
|---------|---------|
| `/start` | Запустить бота, получить приветствие |
| `/hello` | Альтернатива для `/start` |
| Обычное сообщение | Пересылается в чат поддержки |

### Команды для операторов

| Команда | Описание | Место |
|---------|---------|--------|
| `/export` | Экспортировать данные в Excel | Чат поддержки |
| Reply на сообщение | Отправить ответ клиенту | Чат поддержки |

### Примеры работы

#### Клиент пишет вопрос
```
Клиент: Привет! Когда будет доставка?
```

Результат в чате поддержки:
```
📨 Иван Петров (ID: 12345)
👤 [Иван Петров](max://user/12345)
_Вопрос пользователя:_

**Привет! Когда будет доставка?**

💬 Ответов предоставлено: 0
```

#### Оператор отвечает
```
Оператор (Reply): Доставка займет 2-3 дня!
```

Клиент получает:
```
💬 Доставка займет 2-3 дня!
```

#### Экспорт данных
```
Оператор (в чате): /export
```

Результат:
- Создается Excel файл с двумя листами: "Пользователи" и "Сообщения"
- Файл загружается и отправляется в чат
- Excel файл может быть скачан оператором

---

## 📚 API Reference

### BotService

```python
class BotService:
    def start() -> None
        """Запустить бота в режиме long polling"""
    
    def stop() -> None
        """Остановить бота"""
    
    def set_update_handler(handler: UpdateHandler) -> None
        """Установить обработчик событий"""
```

### UserService

```python
class UserService:
    def register_or_update_user(user_id: int, name: str) -> User
        """Зарегистрировать или обновить пользователя"""
    
    def send_welcome_message(user_id: int, name: str) -> None
        """Отправить приветственное сообщение"""
    
    def handle_start_command(user_id: int, name: str) -> None
        """Обработать команду /start"""
    
    def handle_bot_started(user_id: int, name: str) -> None
        """Обработать событие запуска бота"""
```

### MessageService

```python
class MessageService:
    def save_user_message(user_id: int, text: str) -> Message
        """Сохранить сообщение от пользователя"""
    
    def save_operator_message(user_id: int, text: str, operator_name: str) -> Message
        """Сохранить сообщение от оператора"""
    
    def forward_to_support(user_id: int, user_name: str, text: str) -> Optional[str]
        """Переслать сообщение в чат поддержки"""
    
    def send_operator_reply(user_id: int, user_name: str, operator_name: str, text: str) -> bool
        """Отправить ответ оператора клиенту"""
```

### MaxApiClient

```python
class MaxApiClient:
    def get_updates(marker: Optional[str], timeout: int) -> dict
        """Получить обновления через long polling"""
    
    def send_message_to_user(user_id: int, text: str) -> dict
        """Отправить сообщение пользователю"""
    
    def send_message_to_chat(chat_id: int, text: str) -> dict
        """Отправить сообщение в групповой чат"""
    
    def upload_file(file_path: str) -> str
        """Загрузить файл на сервер"""
    
    def send_file_to_chat(chat_id: int, file_token: str, text: str, filename: str) -> dict
        """Отправить файл в чат"""
```

---

## 📁 Структура проекта

```
lavita-bot/
├── src/
│   ├── config/
│   │   └── settings.py              # Конфигурация приложения
│   ├── database/
│   │   └── connection.py            # Менеджер подключения к БД
│   ├── models/
│   │   ├── user.py                  # Доменная модель User
│   │   ├── message.py               # Доменная модель Message
│   │   └── update.py                # Доменная модель Update
│   ├── repositories/
│   │   ├── user_repository.py       # Репозиторий пользователей
│   │   └── message_repository.py    # Репозиторий сообщений
│   ├── services/
│   │   ├── bot_service.py           # Главный сервис бота
│   │   ├── user_service.py          # Сервис пользователей
│   │   ├── message_service.py       # Сервис сообщений
│   │   └── export_service.py        # Сервис экспорта в Excel
│   ├── handlers/
│   │   └── update_handler.py        # Обработчик событий
│   └── clients/
│       └── max_api_client.py        # Клиент Max.ru API
├── main.py                          # Точка входа приложения
├── .env                             # Переменные окружения
├── .env.example                     # Пример конфигурации
└── README.md                        # Данная документация
```

### Описание ключевых файлов

#### `main.py` — точка входа
Инициализирует все компоненты и запускает бота с внедрением зависимостей.

#### `config/settings.py` — конфигурация
Загружает переменные окружения и предоставляет их приложению.

#### `models/` — доменные модели
Содержат dataclass модели для типобезопасности:
- `User` — пользователь системы
- `Message` — сообщение в истории
- `Update` — событие от API

#### `repositories/` — слой доступа к данным
Абстрактные интерфейсы и реализации для SQLite:
- `IUserRepository` / `SQLiteUserRepository`
- `IMessageRepository` / `SQLiteMessageRepository`

#### `services/` — бизнес-логика
Сервисы с основной логикой приложения.

#### `handlers/` — обработчики событий
`UpdateHandler` маршрутизирует события по типам и делегирует обработку сервисам.

---

## 🔧 Разработка

### Добавление новой функции

1. Создайте модель в `models/` если нужна новая сущность
2. Добавьте методы в репозиторий `repositories/`
3. Добавьте бизнес-логику в сервис `services/`
4. Добавьте обработку в `handlers/update_handler.py`

### Тестирование

Рекомендуется добавить unit-тесты благодаря использованию интерфейсов:

```python
# Пример: тестирование UserService
class TestUserService:
    def test_register_new_user(self, mock_repository, mock_api):
        service = UserService(mock_repository, mock_api, settings)
        user = service.register_or_update_user(123, "Test User")
        assert user.name == "Test User"
```

---

## 🐛 Отладка

### Debug режим

Включите DEBUG в `.env`:
```env
DEBUG=true
```

При ошибке вы увидите полный traceback.

### Логирование

Для логирования используйте `print()`. В будущем рекомендуется перейти на модуль `logging`:

```python
import logging

logger = logging.getLogger(__name__)
logger.info("Сообщение")
logger.error("Ошибка")
```

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| `ValueError: Обязательная переменная окружения не установлена` | Проверьте файл `.env` |
| `sqlite3.OperationalError: database is locked` | Убедитесь, что бот не запущен в нескольких процессах |
| `MaxApiTimeoutError` | Это нормально при long polling, бот сам восстановится |

---

## 📊 База данных

### Схема SQLite

#### Таблица `users`
```sql
CREATE TABLE users (
    user_id INTEGER PRIMARY KEY,
    name TEXT NOT NULL,
    first_contact TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    last_contact TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

#### Таблица `messages`
```sql
CREATE TABLE messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    text TEXT NOT NULL,
    direction TEXT NOT NULL,  -- 'from_user' или 'to_user'
    operator_name TEXT,
    timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(user_id)
);
```

#### Таблица `message_mapping`
```sql
CREATE TABLE message_mapping (
    message_id TEXT PRIMARY KEY,
    user_id INTEGER NOT NULL,
    user_name TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 📝 Лицензия

Проект LaVita Yarn Support Bot. Все права защищены.

---

## 🤝 Контакты

Для вопросов и предложений:
- 📧 Email: support@lavita.ru
- 💬 Telegram: @lavita_support

---

**Версия:** 1.0.0  
**Последнее обновление:** Январь 2026  
**Статус:** Production Ready ✅