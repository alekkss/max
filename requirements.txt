# HTTP клиент для запросов к Max.ru API
requests==2.31.0

# Управление настройками через переменные окружения
pydantic-settings==2.1.0

# Валидация данных (устанавливается автоматически с pydantic-settings, но указываем явно)
pydantic==2.5.3
